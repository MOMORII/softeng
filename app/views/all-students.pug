extend layout

block content
    - console.log(data)
    .main-content
    h1 All students
    h1 All students table(border=1)
    table(border=1)
    for item in data
        tr
            td #{item.id}
            td 
                a(href='/student-single/'+ item.id) #{item.name}


          




